# 3.2 Cài đặt triển khai ứng dụng

Phần cài đặt triển khai ứng dụng là bước chuyển đổi các kết quả nghiên cứu và thiết kế ở những phần trước thành một hệ thống phần mềm có thể vận hành thực tế. Hệ thống được xây dựng theo mô hình ứng dụng Web full-stack, trong đó Backend đảm nhiệm việc xử lý nghiệp vụ, kết nối cơ sở dữ liệu và thực thi pipeline trí tuệ nhân tạo; Frontend cung cấp giao diện tương tác cho sinh viên và giảng viên; cơ sở dữ liệu lưu trữ danh sách sinh viên, phiên điểm danh và lịch sử điểm danh. Cách tổ chức này giúp tách biệt rõ trách nhiệm giữa các lớp xử lý, thuận lợi cho quá trình phát triển, kiểm thử và mở rộng hệ thống về sau.

Về mặt triển khai, ứng dụng gồm hai khối chính. Khối Backend được phát triển bằng Python với framework FastAPI, kết hợp SQLAlchemy để thao tác với cơ sở dữ liệu quan hệ thông qua biến môi trường `DATABASE_URL`. Khối Frontend được xây dựng bằng ReactJS trên nền Vite, sử dụng `react-webcam` để truy cập webcam và `react-router-dom` để tổ chức các trang chức năng. Mô hình nhận diện YOLO sau huấn luyện được đặt tại thư mục `backend/AI_service/weights/best.pt` và được nạp khi dịch vụ Backend khởi động. Các thư viện xử lý ảnh và nhận dạng ký tự như OpenCV, Ultralytics YOLO và PaddleOCR được tích hợp trong pipeline AI để thực hiện nhận diện mã số sinh viên từ ảnh thẻ.

Để triển khai hệ thống trên môi trường cục bộ, trước hết cần chuẩn bị Python, Node.js và một hệ quản trị cơ sở dữ liệu tương thích với chuỗi kết nối cấu hình trong `DATABASE_URL`. Ở phía Backend, lập trình viên tạo môi trường ảo Python, cài đặt các thư viện trong `requirements.txt`, bổ sung các gói phục vụ OCR nếu môi trường chưa có PaddleOCR, sau đó chạy ứng dụng bằng Uvicorn. Khi Backend khởi động, FastAPI đăng ký các router quản lý sinh viên và điểm danh, đồng thời SQLAlchemy tự động tạo các bảng cần thiết nếu chưa tồn tại. Ở phía Frontend, lập trình viên cài đặt các package bằng `npm install` và khởi chạy giao diện bằng `npm run dev`. Sau khi hai dịch vụ hoạt động, người dùng có thể truy cập trang quét thẻ tại đường dẫn gốc của Frontend và trang quản trị tại `/dashboard`.

## 3.2.1 Cài đặt Backend và cơ sở dữ liệu

Backend là trung tâm xử lý của hệ thống, được đặt trong thư mục `backend/app`. Tệp `main.py` khởi tạo ứng dụng FastAPI với tên "Hệ thống Điểm danh PTIT", cấu hình CORS để Frontend có thể gọi API, đăng ký router `/students` cho nhóm chức năng quản lý sinh viên và router `/attendance` cho nhóm chức năng điểm danh. Việc tách các router theo miền nghiệp vụ giúp mã nguồn dễ bảo trì, đồng thời làm rõ luồng xử lý của từng chức năng.

Tầng cơ sở dữ liệu được xây dựng bằng SQLAlchemy. Tệp `database.py` đọc biến môi trường `DATABASE_URL`, tạo `engine`, `SessionLocal` và hàm phụ thuộc `get_db()` để cấp phiên làm việc cơ sở dữ liệu cho từng request. Khi ứng dụng khởi động, lệnh `Base.metadata.create_all(bind=engine)` tạo các bảng theo model đã khai báo. Ngoài ra, tệp `migrations.py` bổ sung một số thay đổi cần thiết cho cơ sở dữ liệu cũ, chẳng hạn thêm `session_id`, `status` cho bảng điểm danh và thêm `is_active` cho bảng sinh viên. Cách triển khai này giúp hệ thống vận hành ổn định hơn trong quá trình phát triển khi cấu trúc dữ liệu có thay đổi.

Hệ thống sử dụng ba nhóm bảng chính. Bảng `students` lưu thông tin sinh viên gồm mã sinh viên, họ tên và trạng thái hoạt động. Trạng thái `is_active` được dùng để xóa mềm sinh viên, nhờ đó sinh viên có thể bị loại khỏi danh sách lớp hiện tại mà lịch sử điểm danh cũ vẫn được giữ nguyên. Bảng `attendance_sessions` lưu thông tin từng buổi học, gồm tên buổi, thời điểm bắt đầu, thời điểm kết thúc và trạng thái đang hoạt động. Bảng `attendance` lưu từng lượt điểm danh, liên kết mã sinh viên với phiên điểm danh tương ứng, kèm thời gian ghi nhận và trạng thái như "Có mặt" hoặc "Đi muộn". Ràng buộc duy nhất trên cặp `student_code` và `session_id` giúp ngăn một sinh viên điểm danh nhiều lần trong cùng một buổi học.

Nhóm API quản lý sinh viên cho phép thêm, sửa, xóa mềm, tìm kiếm và lấy trạng thái điểm danh của sinh viên. Trước khi lưu dữ liệu, Backend chuẩn hóa mã sinh viên về chữ in hoa và kiểm tra định dạng bằng biểu thức chính quy. Mã sinh viên hợp lệ có dạng một chữ cái, hai chữ số, bốn chữ cái và ba chữ số, ví dụ `B23DCCN313`. Việc kiểm tra ở Backend giúp tránh dữ liệu sai định dạng ngay cả khi người dùng bỏ qua các kiểm tra ở giao diện.

Nhóm API điểm danh quản lý toàn bộ vòng đời của một buổi học. Giảng viên có thể khởi tạo buổi học mới, lấy buổi học đang mở, kết thúc buổi học, xem lịch sử từng buổi và xuất kết quả điểm danh ra tệp Excel. Khi một buổi học được khởi tạo, các lượt điểm danh tiếp theo đều được gắn với `session_id` của buổi đó. Khi kết thúc buổi học, hệ thống đóng phiên hiện tại, lưu lại thời điểm kết thúc và giữ dữ liệu để phục vụ tra cứu lịch sử.

## 3.2.2 Pipeline AI nhận diện thẻ sinh viên

Pipeline AI là thành phần cốt lõi giúp hệ thống tự động đọc mã số sinh viên từ ảnh thẻ. Quá trình xử lý được triển khai trong các tệp `yolo_service.py`, `ocr_service.py` và `pipeline_service.py`. Thay vì đưa toàn bộ ảnh thẻ trực tiếp vào OCR, hệ thống chia bài toán thành hai bước: trước hết dùng YOLO để định vị vùng chứa mã số sinh viên, sau đó chỉ cắt vùng ảnh cần thiết và đưa vào PaddleOCR để nhận dạng ký tự. Cách tiếp cận này giúp giảm nhiễu nền, tăng độ chính xác đọc chữ và rút ngắn thời gian xử lý.

Ở bước phát hiện vùng ảnh, lớp `YOLOService` nạp mô hình đã huấn luyện từ đường dẫn `AI_service/weights/best.pt`. Khi nhận ảnh đầu vào, mô hình YOLO được gọi với ngưỡng tin cậy `conf=0.4` để tìm các hộp giới hạn. Trong các kết quả phát hiện, hệ thống chỉ lấy hộp có nhãn `mssv`, tức vùng chứa mã số sinh viên trên thẻ. Nếu mô hình không phát hiện được vùng này, pipeline trả về `None` và API phản hồi rằng hệ thống chưa đọc được mã sinh viên, yêu cầu người dùng thử lại.

Sau khi YOLO trả về tọa độ hộp giới hạn, hệ thống không cắt nguyên toàn bộ vùng phát hiện mà dịch mép trên của vùng cắt xuống khoảng 35% chiều cao hộp. Mục đích của thao tác này là loại bỏ phần nhãn chữ như "Mã SV" và tập trung vào chuỗi mã số thật sự. Vùng cắt cũng được mở rộng thêm một phần đệm nhỏ theo chiều ngang và chiều dọc để tránh mất ký tự ở rìa ảnh. Ảnh sau khi cắt trở thành đầu vào cho bước tiền xử lý trước OCR.

Hàm `preprocess_for_ocr()` thực hiện các bước xử lý ảnh nhằm làm rõ ký tự trên thẻ. Ảnh được chuyển sang ảnh xám, phóng to bằng nội suy bicubic để ký tự rõ hơn, tăng tương phản bằng CLAHE, khử nhiễu bằng thuật toán fast non-local means, nhị phân hóa thích nghi để tách chữ khỏi nền và làm sắc nét bằng phép kết hợp ảnh gốc với ảnh làm mờ Gaussian. Cuối cùng, ảnh được chuyển lại về định dạng màu BGR để PaddleOCR nhận đầu vào ổn định hơn. Chuỗi xử lý này đặc biệt hữu ích trong điều kiện lớp học có ánh sáng không đều, thẻ bị bóng hoặc ảnh webcam chưa thật sắc nét.

Ở bước nhận dạng ký tự, lớp `OCRService` khởi tạo PaddleOCR với tùy chọn nhận diện hướng chữ và ngôn ngữ tiếng Anh, phù hợp với cấu trúc mã sinh viên gồm chữ cái Latin và chữ số. Kết quả OCR có thể chứa khoảng trắng hoặc ký tự thừa, do đó hệ thống tiếp tục hậu xử lý bằng cách xóa khoảng trắng, chuyển toàn bộ về chữ in hoa và tìm chuỗi khớp với mẫu mã sinh viên bằng biểu thức chính quy `[A-Z]\d{2}[A-Z]{4}\d{3}`. Nếu tìm được chuỗi hợp lệ, pipeline trả về mã sinh viên cuối cùng cho API điểm danh. Nếu không khớp hoàn toàn, hệ thống trả về chuỗi đã làm sạch để phục vụ chẩn đoán hoặc tiếp tục xử lý ở các bước sau.

## 3.2.3 Luồng điểm danh tự động

Luồng điểm danh tự động bắt đầu từ giao diện quét thẻ của sinh viên. Trên trình duyệt, component quét thẻ sử dụng webcam để lấy hình ảnh theo chu kỳ khoảng 1,5 giây. Mỗi khung hình được chuyển thành dữ liệu ảnh JPEG, đóng gói dưới dạng `FormData` và gửi tới API `/attendance/checkin` của Backend. Về mặt triển khai, WebSocket không trực tiếp truyền toàn bộ luồng video; thay vào đó, Frontend gửi các ảnh chụp định kỳ qua HTTP để Backend xử lý AI, còn WebSocket được dùng để đồng bộ trạng thái điểm danh theo thời gian thực cho giao diện quản lý.

Khi API `/attendance/checkin` nhận ảnh, hệ thống lưu ảnh tạm thời trên máy chủ, kiểm tra xem hiện có buổi học nào đang mở hay không, sau đó gọi pipeline AI để trích xuất mã sinh viên. Nếu chưa có buổi học đang mở, API trả về mã lỗi `NO_ACTIVE_SESSION`. Nếu ảnh không đọc được mã sinh viên, API trả về thông báo yêu cầu thử lại. Nếu đọc được mã nhưng mã đó không tồn tại trong danh sách lớp, hệ thống trả về trạng thái sinh viên không thuộc lớp. Trường hợp mã sinh viên hợp lệ và có trong lớp, Backend kiểm tra sinh viên đã điểm danh trong buổi hiện tại hay chưa để tránh ghi nhận trùng.

Khi lượt điểm danh hợp lệ, Backend ghi một bản ghi mới vào bảng `attendance` với trạng thái mặc định là `present`. Sau khi lưu thành công, hệ thống phát sự kiện `CHECKIN_SUCCESS` qua WebSocket tới các dashboard đang mở. Nhờ vậy, giảng viên không cần tải lại trang vẫn thấy trạng thái sinh viên chuyển sang "Có mặt" gần như ngay lập tức. Sau khi request kết thúc, ảnh tạm được xóa khỏi máy chủ nhằm hạn chế lưu trữ không cần thiết các khung hình có chứa thông tin cá nhân.

Luồng tự động còn xử lý các tình huống đặc biệt trong quá trình vận hành. Nếu sinh viên đã điểm danh, Frontend hiển thị cảnh báo "Bạn đã điểm danh rồi" và tạm dừng quét trong vài giây để tránh gửi lặp liên tục. Nếu điểm danh thành công, giao diện phát âm báo ngắn, hiển thị mã sinh viên vừa nhận diện và cũng đặt thời gian chờ trước khi quét lượt tiếp theo. Cơ chế này giúp quy trình điểm danh diễn ra tự nhiên, tránh tình trạng một thẻ bị gửi quá nhiều lần trong thời gian ngắn.

## 3.2.4 Giao diện điểm danh sinh viên

Giao diện điểm danh sinh viên được triển khai tại trang gốc của ứng dụng Frontend. Màn hình này được thiết kế tập trung vào nhiệm vụ quét thẻ, gồm tiêu đề, vùng hiển thị webcam, khung căn chỉnh ở giữa màn hình, hiệu ứng đường quét và khu vực thông báo trạng thái. Sinh viên chỉ cần đưa thẻ vào vùng hướng dẫn trên màn hình, hệ thống sẽ tự động chụp ảnh theo chu kỳ và gửi tới Backend mà không cần thao tác bấm nút thủ công.

Khu vực hiển thị webcam là thành phần quan trọng nhất của giao diện. Khung căn chỉnh giúp sinh viên đặt thẻ ở khoảng cách và vị trí phù hợp, từ đó vùng chứa mã số sinh viên rơi vào khu vực dễ nhận diện của mô hình. Khi hệ thống chưa đọc được thẻ, giao diện hiển thị trạng thái "Đang quét thẻ..." để người dùng biết quá trình vẫn đang diễn ra. Khi quét thành công, thông báo chuyển sang trạng thái thành công, hiển thị chính xác mã sinh viên vừa nhận diện và phát âm thanh phản hồi ngắn. Nếu có lỗi nghiệp vụ như chưa mở buổi học hoặc sinh viên đã điểm danh, giao diện hiển thị cảnh báo rõ ràng để người dùng điều chỉnh thao tác.

Việc tự động chụp ảnh theo chu kỳ giúp giảm thao tác cho sinh viên, đồng thời phù hợp với bối cảnh lớp học đông người. Thay vì mỗi sinh viên phải bấm nút gửi ảnh, hệ thống tự động kiểm tra khung hình hiện tại, nhận diện mã sinh viên và chuyển sang trạng thái chờ lượt tiếp theo. Cách thiết kế này góp phần rút ngắn thời gian điểm danh, đặc biệt khi nhiều sinh viên lần lượt đưa thẻ qua camera.

## 3.2.5 Giao diện quản lý điểm danh trong buổi học hiện tại

Giao diện quản lý của giảng viên được đặt tại đường dẫn `/dashboard`. Trong dashboard, mục "Điểm danh" cung cấp cái nhìn tổng quan về buổi học hiện tại. Các chỉ số như sĩ số, số sinh viên đã có mặt, số sinh viên đi muộn và số sinh viên vắng được hiển thị ở phần đầu trang. Bên dưới là bảng danh sách sinh viên, trong đó mỗi dòng thể hiện họ tên, mã sinh viên, trạng thái điểm danh và các thao tác xử lý thủ công.

Trước khi bắt đầu quét thẻ, giảng viên cần khởi tạo một buổi học mới. Khi nhấn nút khởi tạo, Frontend gọi API `/attendance/sessions` để tạo phiên điểm danh. Từ thời điểm này, mọi lượt quét thẻ hợp lệ đều được gắn với phiên vừa tạo. Nếu chưa có buổi học đang mở, hệ thống vẫn hiển thị danh sách sinh viên nhưng trạng thái là "Chưa mở buổi", đồng thời không cho ghi nhận điểm danh cho đến khi phiên được khởi tạo. Khi buổi học kết thúc, giảng viên nhấn "Kết thúc buổi học" để đóng phiên và lưu lịch sử.

Dashboard cũng hỗ trợ điểm danh thủ công trong các tình huống ngoại lệ, chẳng hạn sinh viên quên thẻ, thẻ bị hỏng hoặc camera không đọc được mã do điều kiện môi trường. Với mỗi sinh viên chưa được ghi nhận, giảng viên có thể chọn "Có mặt" hoặc "Đi muộn". Thao tác này gọi API `/attendance/manual`, lưu bản ghi điểm danh tương tự như luồng AI và cũng phát sự kiện WebSocket để các trạng thái trên giao diện được cập nhật thống nhất. Nhờ chức năng này, hệ thống vẫn linh hoạt trong thực tế vận hành, không phụ thuộc tuyệt đối vào khả năng nhận diện của mô hình.

## 3.2.6 Giao diện quản lý sinh viên

Mục "Sinh viên" trong dashboard phục vụ quản lý danh sách lớp. Giao diện gồm biểu mẫu thêm sinh viên mới và bảng danh sách sinh viên hiện có. Khi thêm sinh viên, người dùng nhập mã sinh viên và họ tên; Frontend kiểm tra dữ liệu cơ bản như không để trống, mã sinh viên có đúng 10 ký tự và họ tên có độ dài hợp lệ. Sau đó dữ liệu được gửi tới Backend để tiếp tục kiểm tra định dạng bằng biểu thức chính quy và kiểm tra trùng mã sinh viên.

Bảng danh sách sinh viên hỗ trợ tìm kiếm theo mã sinh viên hoặc họ tên, giúp giảng viên dễ dàng quản lý lớp có số lượng sinh viên lớn. Với từng sinh viên, hệ thống cho phép sửa mã sinh viên, sửa họ tên hoặc xóa khỏi danh sách lớp. Chức năng xóa được triển khai theo cơ chế xóa mềm, tức sinh viên được đánh dấu không còn hoạt động thay vì bị xóa vật lý khỏi cơ sở dữ liệu. Cách làm này bảo toàn lịch sử điểm danh của các buổi học trước, đồng thời vẫn cho phép khôi phục sinh viên nếu thêm lại cùng mã sinh viên.

Khi cập nhật mã sinh viên, Backend kiểm tra thêm trường hợp sinh viên đã có lịch sử điểm danh. Nếu đã tồn tại lịch sử, hệ thống không cho đổi mã sinh viên để tránh làm sai lệch dữ liệu quá khứ. Đây là một ràng buộc cần thiết vì bảng điểm danh liên kết với sinh viên thông qua mã sinh viên. Nhờ vậy, dữ liệu trong các phiên điểm danh cũ vẫn nhất quán và có thể truy vết chính xác.

## 3.2.7 Giao diện lịch sử điểm danh và xuất file Excel

Mục "Lịch sử" trong dashboard cho phép giảng viên xem lại kết quả của các buổi học đã được tạo. Giao diện hiển thị danh sách các phiên điểm danh, thông tin thời gian bắt đầu, thời gian kết thúc và số lượng sinh viên đã tham gia. Khi chọn một buổi học, Frontend gọi API `/attendance/history/{session_id}` để lấy thông tin chi tiết gồm danh sách sinh viên, trạng thái từng sinh viên và thời điểm điểm danh nếu có.

Trên bảng lịch sử, mỗi sinh viên được gắn nhãn trạng thái rõ ràng: "Có mặt", "Đi muộn" hoặc "Vắng". Cách hiển thị này giúp giảng viên nhanh chóng đánh giá tình hình tham gia lớp học của từng buổi, đồng thời có thể đối chiếu lại khi cần xử lý khiếu nại hoặc tổng hợp dữ liệu cuối kỳ. Các sinh viên đã bị xóa mềm khỏi danh sách hiện tại vẫn có thể xuất hiện trong lịch sử của những buổi học cũ, nhờ đó dữ liệu quá khứ không bị mất.

Chức năng xuất Excel được triển khai tại API `/attendance/history/{session_id}/export`. Backend sử dụng thư viện OpenPyXL để tạo tệp `.xlsx` trực tiếp trên bộ nhớ. Tệp xuất gồm tiêu đề kết quả điểm danh, thông tin buổi học, thời gian bắt đầu - kết thúc, tổng sĩ số, số sinh viên có mặt, đi muộn, vắng và bảng chi tiết từng sinh viên. Các cột trong bảng gồm số thứ tự, mã sinh viên, họ tên, trạng thái và thời gian điểm danh. Tệp Excel được trả về dưới dạng streaming response để trình duyệt tải xuống, phục vụ lưu trữ hoặc nộp báo cáo cho giảng viên.

## 3.2.8 Nhận xét về kết quả triển khai

Sau quá trình cài đặt, hệ thống đã hình thành đầy đủ các chức năng chính của một ứng dụng điểm danh bằng thẻ sinh viên. Pipeline AI đảm nhiệm việc nhận diện mã số sinh viên từ ảnh webcam; Backend kiểm soát nghiệp vụ, cơ sở dữ liệu và lịch sử điểm danh; Frontend cung cấp hai luồng giao diện riêng cho sinh viên và giảng viên. Việc kết hợp HTTP cho xử lý ảnh với WebSocket cho cập nhật trạng thái giúp hệ thống đạt được trải nghiệm gần thời gian thực nhưng vẫn giữ kiến trúc triển khai đơn giản, dễ kiểm soát và phù hợp với phạm vi đề tài.

Hệ thống cũng đã tính đến các tình huống vận hành thực tế như chưa mở buổi học, điểm danh trùng, sinh viên không thuộc lớp, đọc thẻ thất bại, điểm danh thủ công và xuất lịch sử ra Excel. Các cơ chế này làm cho ứng dụng không chỉ dừng lại ở phần trình diễn mô hình AI mà trở thành một công cụ quản lý điểm danh tương đối hoàn chỉnh, có khả năng áp dụng trong môi trường lớp học quy mô nhỏ đến trung bình. Đây là cơ sở quan trọng để tiếp tục mở rộng các chức năng nâng cao như xác thực khuôn mặt, triển khai trên máy chủ tập trung hoặc phát triển phiên bản di động trong tương lai.
